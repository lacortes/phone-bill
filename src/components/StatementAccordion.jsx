import styled from '@emotion/styled';
import { Accordion } from '@mui/material';

const StatementAccordion = styled(Accordion)({ border: 'solid #000000 1px', boxShadow: 'none' });

export default StatementAccordion;